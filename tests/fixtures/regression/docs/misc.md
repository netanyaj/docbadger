# Misc

## Notifications
Call `send_notification(user, message)` to notify a user of an event.

## Configuration
Call `load_configuration()` to load application settings. (Note: this
name is intentionally stale/mismatched for regression testing — the
real function is load_settings.)
