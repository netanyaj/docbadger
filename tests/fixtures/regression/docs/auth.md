# Auth

## Login
Call `login(username, password)` to authenticate a user.

### Session Handling
Call `logout(session_id)` to end a session once the user is done.

## Registration
Call `register(username, password, email)` to create a new account.
