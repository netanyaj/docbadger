{
  "id": "002_synthetic_delegated_behavior_accuracy_rejection",
  "source": {
    "type": "synthetic",
    "modeled_after": "A real self-test run on docbadger's own fixtures/sample_module.py (PR test_trigger_workflow). send_email() gained a `retries` parameter forwarded to an external, undefined `smtp_client.send`. The Corrector asserted the function 'handles retry attempts' — an unprovable claim, since smtp_client's implementation isn't visible anywhere in the codebase. The Validator correctly rejected this on accuracy grounds. Rebuilt here as a standalone synthetic case so this exact pattern has permanent regression coverage independent of whether the live fixture changes later."
  },
  "old_code": "def send_email(to, subject, body):\n    \"\"\"Send an email. Does not retry on failure.\"\"\"\n    smtp_client.send(to, subject, body)",
  "new_code": "def send_email(to, subject, body, retries):\n    \"\"\"Send an email. Does not retry on failure.\"\"\"\n    smtp_client.send(to, subject, body, retries)",
  "stale_doc_section": "Call `send_email(to, subject, body)` to send an email. If sending fails, it\nwill not retry — you must handle retries yourself.",
  "human_fixed_doc_section": null,
  "labels": {
    "verifier_expected_stale": true,
    "expected_pipeline_outcome": {
      "corrector_status": "proposed",
      "validator_status": "rejected_accuracy",
      "final_comment_kind": "flagged_rejected"
    },
    "notes_for_labeler": "There is no single 'correct' human_fixed_doc_section here — the honest fix is under-specified because smtp_client's real retry behavior is genuinely unknown from this code alone. The point of this case isn't 'what's the right correction' but 'does the pipeline correctly refuse to assert something it can't verify.' A Corrector output that hedges (e.g. '...now accepts a `retries` parameter, passed to the underlying send call' rather than '...handles retry attempts') should arguably pass Validator — see Future Product Opportunities #10. This case is a good regression check for whichever way that prompt-refinement decision eventually goes.\n\nFLAGGED (this session's rerun, gold label NOT changed pending investigation): a fresh run against the current pipeline/model produced corrector_status='abstained_diagnosis' instead of the established 'proposed'->'rejected_accuracy' gold path -- the corrector's real rationale this time reads as recognizing the same ambiguity the gold label was designed around ('the function signature suggests retry capability, but the internal documentation contradicts it, making it unclear what the intended behavior is'), but chose to abstain rather than propose-then-get-rejected. Both outcomes are arguably reasonable responses to the same genuine ambiguity -- this may be legitimate LLM non-determinism on a boundary case rather than a regression, but it's a real, observed divergence from a previously CONFIRMED result and should not be quietly ignored. Recommend: rerun this case 2-3 more times before deciding whether to update the gold label, note the model/date of each run (OpenRouter's 'openai/gpt-4o' alias can route to an updated underlying model version without a version bump on your end)."
  }
}