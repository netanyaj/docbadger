# Sample Docs

## Sending Emails
Call `send_email(to, subject, body)` to send an email. If sending fails, it
will not retry — you must handle retries yourself.

## Unrelated Section
This section exists just to prove heading-boundary extraction works
correctly and doesn't bleed into neighboring sections.
